const initialstate = [];

const reducer = (state, action) => {
  switch (action.type) {
    case "":
      return state;

    case "":
      return state;

    default:
      return state;
  }
};

export default reducer;
